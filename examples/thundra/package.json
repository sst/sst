{
  "name": "thundra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@serverless-stack/cli": "0.68.0",
    "@serverless-stack/resources": "0.68.0",
    "aws-cdk-lib": "2.15.0"
  },
  "devDependencies": {
    "@thundra/esbuild-plugin": "^1.0.3"
  }
}
