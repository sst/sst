{
  "name": "flutter-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "dependencies": {
    "aws-cdk-lib": "2.7.0",
    "@serverless-stack/cli": "0.60.4",
    "@serverless-stack/resources": "0.60.4",
    "aws-sdk": "^2.1010.0"
  }
}
