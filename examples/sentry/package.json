{
  "name": "sentry",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "dependencies": {
    "@sentry/serverless": "^6.14.1",
    "@serverless-stack/cli": "0.60.8",
    "@serverless-stack/resources": "0.60.8",
    "aws-cdk-lib": "2.7.0"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  }
}
