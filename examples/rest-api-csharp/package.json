{
  "name": "rest-api-csharp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.114.0"
  },
  "dependencies": {
    "@serverless-stack/cli": "0.40.0",
    "@serverless-stack/resources": "0.40.0"
  }
}
