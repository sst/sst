{
  "name": "svelte-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "dependencies": {
    "@serverless-stack/cli": "0.61.2",
    "@serverless-stack/resources": "0.61.2",
    "aws-cdk-lib": "2.15.0",
    "aws-sdk": "^2.1023.0"
  }
}
