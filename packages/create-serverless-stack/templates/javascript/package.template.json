{
  "name": "%name%",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": ["serverless-stack"]
  },
  "devDependencies": {
    "eslint": "^8.10.0",
    "eslint-config-serverless-stack": "%sst-version%"
  },
  "dependencies": {
    "@serverless-stack/cli": "%sst-version%",
    "@serverless-stack/resources": "%sst-version%",
    "aws-cdk-lib": "%cdk-version%"
  }
}
