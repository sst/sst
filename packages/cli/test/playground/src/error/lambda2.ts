import { APIGatewayProxyResult } from "aws-lambda";

export async function main(): Promise<APIGatewayProxyResult> {
  // Build error
  //new ApiStack(app, "api2

  // TypeCheck error
  //a

  // Lint error
  //if (true) { }

  return {
    statusCode: 200,
    body: "hi",
  };
}
